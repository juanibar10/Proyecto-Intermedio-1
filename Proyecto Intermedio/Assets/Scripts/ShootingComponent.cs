using UnityEngine;

public class ShootingComponent : MonoBehaviour
{
/*
private float speed;
private SkillMovementType movementType;

// Update is called once per frame
void Update()
{
    switch (movementType) {
        case SkillMovementType.Straigth:
            transform.position += transform.forward * speed * Time.deltaTime;
            break;
        case SkillMovementType.Radial:
            //TODO que rote entorno al padre
            break;
        case SkillMovementType.Homing:
            Transform closestEnemy = EnemySpawner.GetClosestEnemyToPlayer();
            if (closestEnemy != null)
            {
                transform.position += (closestEnemy.position - transform.position).normalized * speed * Time.deltaTime;
            }
            break;
        case SkillMovementType.None:
            break;
    }
}

internal void SetBehaviour(SkillMovementBehaviour behaviour)
{
    speed = behaviour.speed;
    movementType = behaviour.type;
}
*/
}
